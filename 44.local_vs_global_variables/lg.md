Local and Global Variables in Python:

Local → variable created INSIDE a function
Global → variable created OUTSIDE a function

# Simple Rule:

Local → born inside function
dies when function ends
only lives inside function

Global → born outside function
lives entire program
accessible everywhere

global keyword → lets you MODIFY
global variable
from inside function
