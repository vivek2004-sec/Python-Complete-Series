# Recursion

Recursion is the process of defining something in terms of itself.

A physical world example would be to place two parallel mirrors facing each other. Any object in between then would be reflected recursively.

- Python Recursive Function
  In python, we know that a function can call other functions. It is even possible for the fucntion to call itself. These types of construct are termed as recursive funtions.
