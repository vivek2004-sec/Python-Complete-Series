static methods: A method that belongs to a class rather than any object from that class (instance) usually
used for general utility functions.
It does not depend on any class data.

Instance methods: Best for operations on instances of the class (objects)
Static methods: Best for utility functions that do not access to class date.

A method that actually defined within class but, the instance of that class (object) can't have it.
