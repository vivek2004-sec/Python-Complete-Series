d = {1: 'Geeks', 2: 'For', 'age':22}

# Iterate over keys
for keys in d:
    print(keys)

#Iterate over values  
for value in d.values():
    print(value)
    
# Iterate over key-value pairs
for key, value in d.items():
    print(f"{keys}:{value}")