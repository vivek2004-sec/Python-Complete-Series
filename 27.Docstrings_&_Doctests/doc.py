def add(a, b):
    """
    Adds two numbers.

    >>> add(2, 3)
    5
    >>> add(10, 20)
    30
    """
    return a + b
# The >>> lines are the inputs and the lines below them are the expected outputs.

print(add(2,3))