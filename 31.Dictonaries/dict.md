# Dictionary :

Dictionary is the data type which stores data in key-value pairs.
-> Keys must be unique(duplicate keys are not allowed) and immutable.
-> Values can be of any data type. mutable/immutable.
-> we can assign tuples as a key cause it is immutable and can't be changed.
-> Unordered as they doesn't have any index values unlike strings, lists and tuples.
